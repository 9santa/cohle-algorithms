#pragma once

#include <algorithm>
#include <array>
#include <bitset>
#include <cstdint>
#include <limits>
#include <vector>

using namespace std;

using u64 = unsigned long long;
using u32 = unsigned int;
using ll = long long;

#define sz(x) int((x).size())
